services:
  db:
    image: database
    container_name: parabible_db
    restart: always
    build: 
      context: ./postgres
    ports: 
      - 5432:5432
    networks:
      - backend
    env_file:
      - ./postgres/env_file
    command: -p 5432

networks:
  backend: {}